
/**
 * Enumeration containing the available vscode extension commands
 */
export enum Commands {
    /**
     * Select and run a single tox env
     */
    select = "python-tox.select",
    /**
     * Select and run multiple tox envs
     */
    selectMultiple = "python-tox.selectMultiple",
    /**
     * Open the official tox doc
     */
    openDocs = "python-tox.openDocs"
}